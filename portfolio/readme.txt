Please install node modules - "npm install"(Need to install nodejs and npm 
$ node -v

See a value like `v8.8.1` or higher

$ npm -v

See a value like `v5.4.2` or higher
Please type "npm install" && "npm start " to start the application.

